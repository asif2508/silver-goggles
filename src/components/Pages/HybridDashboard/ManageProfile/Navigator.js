export const navigateToBasicInfo = (navigate) => {
  navigate("/dashboard/manage-profile");
};

export const navigateToMentorshipInfo = (navigate) => {
  navigate("/dashboard/manage-profile/mentorship-info");
};

export const navigateToEducationInfo = (navigate) => {
  navigate("/dashboard/manage-profile/education-info");
};

export const navigateToExperienceInfo = (navigate) => {
  navigate("/dashboard/manage-profile/experience-info");
};
